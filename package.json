{
  "scripts": {
    "build": "esbuild index.js --bundle --outfile=dist/bundle.js --target=chrome58 --sourcemap --platform=browser --footer:\"js=/* https://github.com/Dohun-choi */\" --define:process.env.NODE_ENV='production' --define:global=window --minify"
  },
  "devDependencies": {
    "@babel/core": "^7.24.6",
    "@babel/preset-env": "^7.24.6",
    "esbuild": "^0.21.4"
  }
}
